'use client';
import { useState } from 'react';
import { ChevronDown } from 'lucide-react';
import { SectionHeading } from '@/components/ui';
const faqs = [['How long does delivery take?', 'Orders usually leave us within 1–2 working days. Standard delivery takes 2–5 working days depending on your location.'], ['Can I return a book?', 'Yes. Unread books can be returned within 14 days of delivery. Contact hello@chapteronebooks.co.za and we’ll help you through the process.'], ['Do you ship outside South Africa?', 'We do. International delivery rates are calculated at checkout, and delivery times vary by destination.'], ['Can I order a book that is not listed?', 'Almost always. Send us a message with the title or ISBN and we’ll see what we can do.'], ['Do you sell eBooks?', 'Selected titles are available as digital editions. Look for the eBook label on a product page.'], ['Can I send a gift directly?', 'Of course. Add the recipient’s address at checkout and include a gift note in the order notes field.']];
export default function FaqPage() { const [open, setOpen] = useState<number | null>(0); return <main className="mx-auto max-w-[1000px] px-5 py-12 md:py-20"><SectionHeading eyebrow="Helpful things" title="Frequently asked questions" copy="A few answers before you settle in." /><div className="mt-12 border-t border-line">{faqs.map(([question, answer], index) => <div key={question} className="border-b border-line"><button onClick={() => setOpen(open === index ? null : index)} className="flex w-full items-center justify-between gap-5 py-6 text-left"><span className="display-title text-2xl md:text-3xl">{question}</span><ChevronDown size={20} className={`shrink-0 transition ${open === index ? 'rotate-180 text-wine' : ''}`} /></button>{open === index && <p className="max-w-2xl pb-7 pr-10 text-sm leading-6 text-ink/65">{answer}</p>}</div>)}</div></main>; }
