'use client';
import Link from 'next/link';
import { ArrowRight, BookOpen, Heart, MapPin, Package, Settings, UserRound } from 'lucide-react';
import { books } from '@/lib/data';
import { formatPrice } from '@/lib/utils';
import { SectionHeading } from '@/components/ui';
export default function DashboardPage() { return <main className="mx-auto max-w-[1440px] px-5 py-12 md:px-10 md:py-20"><SectionHeading eyebrow="Your account" title="Welcome back, Alex." copy="Your books, orders, and saved places — all in one quiet corner." /><div className="mt-12 grid gap-5 lg:grid-cols-[260px_1fr]"><aside className="h-fit rounded-2xl bg-linen p-4"><div className="mb-5 flex items-center gap-3 border-b border-ink/10 px-3 pb-5"><div className="flex h-10 w-10 items-center justify-center rounded-full bg-wine text-sm font-semibold text-white">AR</div><div><p className="text-sm font-semibold">Alex Reader</p><p className="text-xs text-ink/50">alex@example.com</p></div></div>{[[Package, 'Orders', '/dashboard'], [Heart, 'Wishlist', '/wishlist'], [MapPin, 'Addresses', '#'], [BookOpen, 'Recently viewed', '#'], [Settings, 'Account settings', '#']].map(([Icon, label, href]) => <Link key={label as string} href={href as string} className="flex items-center gap-3 rounded-lg px-3 py-3 text-sm text-ink/65 hover:bg-white hover:text-wine"><Icon size={16} />{label as string}</Link>)}</aside><div className="grid gap-5"><section className="rounded-2xl border border-line p-6 md:p-8"><div className="flex items-end justify-between"><div><p className="text-[11px] font-semibold uppercase tracking-[.2em] text-wine">Latest order</p><h2 className="display-title mt-3 text-3xl">On its way to you</h2></div><span className="rounded-full bg-[#e4efe3] px-3 py-1 text-xs font-semibold text-moss">Shipped</span></div><div className="mt-7 flex flex-col gap-5 border-t border-line pt-5 text-sm md:flex-row md:items-center md:justify-between"><div><p className="text-ink/50">Order #CO-10428 · 09 July 2026</p><p className="mt-1 font-semibold">The Everyday Practice, The Summer Book</p></div><span className="font-semibold">{formatPrice(649)}</span></div></section><section><div className="mb-5 flex items-end justify-between"><div><p className="text-[11px] font-semibold uppercase tracking-[.2em] text-wine">Recently viewed</p><h2 className="display-title mt-2 text-4xl">Pick up where you left off.</h2></div><Link href="/shop" className="hidden text-sm font-semibold text-wine md:block">See all <ArrowRight size={15} className="inline" /></Link></div><div className="grid grid-cols-2 gap-4 md:grid-cols-4">{books.slice(0, 4).map((book) => <Link key={book.id} href={`/book/${book.slug}`} className="group"><div className="aspect-[3/4.25] overflow-hidden rounded-lg bg-linen"><img src={book.cover} alt={book.title} className="h-full w-full object-cover transition duration-500 group-hover:scale-[1.03]" /></div><p className="display-title mt-3 text-xl">{book.title}</p><p className="mt-1 text-xs text-ink/50">{book.author}</p></Link>)}</div></section></div></div></main>; }
