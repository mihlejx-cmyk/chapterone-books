import Link from 'next/link';
import { ArrowUpRight } from 'lucide-react';
import { categories } from '@/lib/data';
import { SectionHeading } from '@/components/ui';
export default function CategoriesPage() { return <main className="mx-auto max-w-[1440px] px-5 py-12 md:px-10 md:py-20"><SectionHeading eyebrow="Browse by mood" title="Find your kind of book" copy="Whether you want to disappear into a story, learn something useful, or make dinner a little better." /><div className="mt-12 grid gap-px overflow-hidden rounded-2xl bg-line md:grid-cols-2 lg:grid-cols-3">{categories.map((category, i) => <Link href={`/shop?category=${encodeURIComponent(category.name)}`} key={category.name} className="group flex min-h-[260px] flex-col justify-between bg-white p-7 transition hover:bg-linen md:p-10"><div className="flex justify-between text-ink/35"><span className="display-title text-4xl">0{i + 1}</span><ArrowUpRight className="transition group-hover:-translate-y-1 group-hover:translate-x-1 group-hover:text-wine" /></div><div><h2 className="display-title text-4xl">{category.name}</h2><p className="mt-2 text-sm text-ink/55">{category.count} titles · {category.note}</p></div></Link>)}</div></main>; }
